import { Identifiers } from '@angular/compiler';

export class Product{
    id:number;
    name:string;
    description:string;
    count:number;
    stock:number;
    price:number;
  }
  export class ProductInfo {
    id: number;
    quantity: number;
}

export class count{
  count:number;
}